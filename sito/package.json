{
  "name": "beauty-obsession",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-pages.mjs && vite --host 127.0.0.1",
    "build": "node scripts/generate-pages.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "pages": "node scripts/generate-pages.mjs",
    "check": "node --check src/main.js && node --check src/bloom.js && python3 scripts/check-links.py"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.2.0",
    "@fontsource/cormorant-garamond": "^5.2.0",
    "gsap": "^3.15.0",
    "lenis": "^1.3.26"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  }
}
